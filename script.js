// Simple animation for waving hand in Hero section
const wave = document.querySelector('.wave');
if (wave) {
    setInterval(() => {
        wave.classList.toggle('wave-animate');
    }, 1000);
}

// Simple animation for waving hand in Hero section
const wave = document.querySelector('.wave');
if (wave) {
    setInterval(() => {
        wave.classList.toggle('wave-animate');
    }, 1000);
}
